# SPDX-License-Identifier: FSL-1.1-ALv2
"""Backend integration tests.

`tests/unit` and `tests/property` were already packages; this one was not, so a
shared helper could not be imported from it. `capability.py` lives here.
"""
