# `src/websocket` — structural placeholder

Required by the PRD §8 layout, deferred out of Phase 0.

- **Owning future phase:** Phase 1 (JSON-RPC 2.0 over WSS hub, agent pairing and heartbeat).
- **Phase 0 rule (design §1.3):** structural artifact only. No `__init__.py`, no package docstring module, no importable Python module of any kind.

Phase 0 ships only the agent-side outbound transport mechanics (`agent/internal/connection/`, design §10.5).
